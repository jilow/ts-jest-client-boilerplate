{
  "name": "ts-jest-client-boilterplate",
  "version": "1.0.0",
  "description": "Client boilterplate using typescript & jest",
  "author": "Jiaan",
  "license": "MIT",
  "scripts": {
    "build": "tsc -d --allowJs false",
    "lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}'",
    "test": "npm run lint && jest",
    "test-watch": "npm run lint && jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^23.3.0",
    "@types/jsend": "^1.0.28",
    "@types/node-fetch": "^2.1.1",
    "jest": "^23.1.0",
    "ts-jest": "^22.4.6",
    "tslint": "^5.11.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "jsend": "^1.0.2",
    "node-fetch": "^2.1.2"
  }
}
